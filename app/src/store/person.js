import {reqAllUserData} from "@/api";

const state ={
    userinfo:{},
};
const mutations = {

};
const actions = {
    getUserInfo(context,id){

    }
};

// 对外暴露
export default {
    state,
    mutations,
    actions,
}